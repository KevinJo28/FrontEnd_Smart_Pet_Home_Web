export const handleAdd = async (e, deviceCode, deviceName, deviceType) => {
    e.preventDefault();

    console.log({
        deviceCode,
        deviceName,
        deviceType
    });

    // fetch al backend
};